#!/usr/bin/env bash
set -u

if [[ $# -eq 1 && "$1" == "+%s" ]]; then
  printf '%s\n' "${MOCK_DATE_EPOCH:-1700000000}"
  exit 0
fi

printf 'Unexpected mock date arguments:' >&2
printf ' %s' "$@" >&2
printf '\n' >&2
exit 64
